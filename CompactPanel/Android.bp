android_app {

    name: "CompactPanel",

    static_libs: [
        "VolumePluginCommon",
        "org.lineageos.platform.internal",
    ],

    resource_dirs: ["res"],

    certificate: "platform",
    optimize: {
        enabled: false,
    },
    platform_apis: true,

    srcs: ["src/**/*.java"],

}
